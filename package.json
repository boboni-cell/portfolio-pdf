{
  "name": "portfolio-pdf",
  "version": "1.0.0",
  "description": "",
  "main": "generate-pdf.js",
  "scripts": {
    "build": "cd site && npm ci && npm run build",
    "build:legacy": "rm -rf dist && mkdir -p dist && cp index.html styles.css i18n.js dist/ && cp -R assets dist/assets",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "puppeteer": "^25.1.0"
  }
}
